import {NextResponse} from "next/server";import {z} from "zod";import {signSession} from "@/core/auth/session";
const loginSchema=z.object({email:z.email(),password:z.string().min(8)});
export async function POST(request:Request){const secret=process.env.AUTH_SECRET;if(!secret||secret.length<32)return NextResponse.json({error:"Authentication is not configured"},{status:503});const parsed=loginSchema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:"Invalid request"},{status:400});if(parsed.data.email!=="demo@orvex.local"||parsed.data.password!=="Synthetic!Pass9")return NextResponse.json({error:"Invalid credentials"},{status:401});const token=signSession({userId:"user_demo_admin",tenantId:"tenant_orvex_demo",expiresAt:Date.now()+8*60*60*1000},secret);const response=NextResponse.json({ok:true});response.cookies.set("orvex_session",token,{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/",maxAge:8*60*60});return response}
