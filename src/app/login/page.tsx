"use client";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";
export default function Login(){const router=useRouter();const[error,setError]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const data=new FormData(event.currentTarget);const response=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:data.get("email"),password:data.get("password")})});if(!response.ok){setError("No fue posible iniciar sesión.");return}router.push("/dashboard")}return <main className="login"><section><p className="code">FND-001</p><h1>ORVEX<br/><span>Fundación DR</span></h1><p>Construyendo esperanza. Cambiando vidas.</p></section><form onSubmit={submit}><p className="code">ACCESO SEGURO</p><h2>Bienvenido/a</h2><p>Use la cuenta sintética para acceder al entorno local.</p><label>Correo electrónico<input name="email" type="email" defaultValue="demo@orvex.local" required/></label><label>Contraseña<input name="password" type="password" defaultValue="Synthetic!Pass9" required/></label>{error?<p role="alert" className="error">{error}</p>:null}<button>Iniciar sesión →</button><small>Datos sintéticos únicamente · Acciones auditables</small></form></main>}
